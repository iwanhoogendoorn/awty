node_modules/
.DS_Store
*.log

# Build output. The manifest is the source of truth for the id, name and
# version, so it stays tracked; main.js and styles.css are generated on every
# build and a 3 MB bundle per commit is not history worth keeping.
/awty/*
!/awty/manifest.json
